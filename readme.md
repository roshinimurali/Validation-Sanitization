# Validation Starter app

## Get Started

Please clone this solution.

## Getting Started

- Set up the `.env` file with the database connection (use `env.example`)
- run the following commands: 
   - > `npm i`
   - > `npm run dev`
